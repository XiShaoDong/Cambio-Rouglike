# 遗物 UI（物品栏 + 护盾卡面紫光）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 M4 遗物效果 v1 补充客户端展示层：防守护盾受保护卡格常驻紫色描边光晕 + ◈ 角标，每个玩家区域左侧一个遗物物品栏（透明背景、3 图标槽、`n/3` 计数、程序化图标、Hover Tooltip）。

**Architecture:** 纯客户端 UI，不改服务器/协议/快照结构。数据全部来自现有快照公开字段（`run.relics`/`run.relic_owners`/`players[].protected_slot`）。新增两个可复用控件（`RelicIcon` 程序化绘制、`RelicBar` 组装 3 槽+计数+tooltip），由 `game_view` 在渲染手牌时挂载定位。护盾光晕用卡牌子节点叠加紫色边框（`StyleBoxFlat`，`mouse_filter=IGNORE`），与现有高亮/闪烁逻辑隔离。

**Tech Stack:** Godot 4.6 / GDScript。headless 编译检查 + `tools/run_all_tests.sh --quick` 回归（不启 GUI）。

## Global Constraints

- 纯展示，**不改** `game_state.gd`/`hidden_info.gd`/协议/快照结构/贴牌时序。
- 数据来源只读 `latest_state`（`run.relics` / `run.relic_owners` / `players[].protected_slot`），不新增 RPC。
- 图标**程序化绘制**（`_draw()` 多边形），不引入图片资源。
- 护盾光晕常驻，不随贴牌窗口开关变化；不干扰卡牌点击（`mouse_filter=IGNORE`）。
- 物品栏每玩家区域一个，显示该玩家遗物（公开信息，人人可见）；护盾按 `protected_slot>=0`（生效中）显示，Joker 按 `relic_owners` 持有显示。
- Hover Tooltip 用 Godot 原生 `tooltip_text`（多行：名称+描述+状态）。
- 不启 GUI 验证；每任务后跑 `tools/run_all_tests.sh --quick` 确认不回归。
- 提交分类：`feat`（UI 功能）/`doc`（文档），push `origin/feature/store`。

---

### Task 1: 主题 token + 程序化图标（RelicIcon）

**Files:**
- Modify: `scripts/ui/ui_theme.gd`（dark/light 各加 `relic_shield` token）
- Create: `scripts/ui/relic_icon.gd`（`class_name RelicIcon`）
- Test: `tools/run_all_tests.sh --quick`（编译检查）

**Interfaces:**
- Consumes: `UITheme.color("relic_shield")`（本任务先定义）。
- Produces: `RelicIcon`（`extends Control`），`enum Kind { EMPTY, SHIELD, JOKER }`、`func setup(kind: RelicIcon.Kind, color: Color) -> void`。Task 2/4 依赖。

- [ ] **Step 1: UITheme 加 token**

`scripts/ui/ui_theme.gd` dark 块 `"card_shadow"` 之后加：
```gdscript
		"card_shadow": Color(0, 0, 0, 0.35),
		# 遗物
		"relic_shield": Color("b07be0"),
```
light 块 `"card_shadow"` 之后加：
```gdscript
		"card_shadow": Color(0.227, 0.212, 0.18, 0.15),
		# 遗物
		"relic_shield": Color("8a5bbf"),
```

- [ ] **Step 2: 新建 RelicIcon**

`scripts/ui/relic_icon.gd`：
```gdscript
class_name RelicIcon
extends Control
## 程序化遗物图标：_draw 绘制盾形/五角星/空槽。无图片资源依赖。

enum Kind { EMPTY, SHIELD, JOKER }

var kind := Kind.EMPTY
var color := Color.WHITE

func setup(kind: Kind, color: Color) -> void:
	self.kind = kind
	self.color = color
	queue_redraw()

func _draw() -> void:
	match kind:
		Kind.SHIELD: _draw_shape(_shield_points())
		Kind.JOKER: _draw_shape(_star_points())
		_: _draw_empty()

## 盾形（经典圆盾轮廓，中心 (16,16)，size 默认 32×32）。
func _shield_points() -> PackedVector2Array:
	return PackedVector2Array([
		Vector2(16, 2), Vector2(27, 6), Vector2(27, 18),
		Vector2(16, 30), Vector2(5, 18), Vector2(5, 6),
	])

## 五角星。
func _star_points() -> PackedVector2Array:
	var pts := PackedVector2Array()
	var outer := 13.0
	var inner := 5.5
	for i in 10:
		var ang: float = -PI / 2.0 + i * PI / 5.0
		var r := outer if i % 2 == 0 else inner
		pts.append(Vector2(16.0 + cos(ang) * r, 16.0 + sin(ang) * r))
	return pts

## 多边形：半透明填充 + 实色描边（2px）。
func _draw_shape(points: PackedVector2Array) -> void:
	var fill := Color(color)
	fill.a = 0.35
	draw_colored_polygon(points, fill)
	draw_polyline(points, color, 2.0, true)

## 空槽：极淡空心圆角方框。
func _draw_empty() -> void:
	var rect := Rect2(Vector2(3, 3), size - Vector2(6, 6))
	var outline := Color(color)
	outline.a = 0.35
	draw_rect(rect, Color(0, 0, 0, 0), false, 1.5)
	draw_rect(rect, outline, false, 1.5)
```

- [ ] **Step 3: 编译检查 + 回归**

Run:
```bash
tools/run_all_tests.sh --quick
```
Expected: 编译检查无脚本错误；单进程核心全绿（12/12，含 verify_relics 19/19）。

- [ ] **Step 4: 提交**

```bash
git add scripts/ui/ui_theme.gd scripts/ui/relic_icon.gd
git commit -m "feat: 程序化遗物图标 RelicIcon（盾/星/空槽）+ UITheme relic_shield 紫色 token"
```
若 Godot 生成 `relic_icon.gd.uid`，一并 add。

---

### Task 2: 遗物物品栏（RelicBar 场景实体 + 脚本）

**Files:**
- Create: `scenes/ui/relic_bar.tscn`、`scripts/ui/relic_bar.gd`（`class_name RelicBar`）
- Test: `tools/run_all_tests.sh --quick`（编译检查）

**Interfaces:**
- Consumes: `RelicIcon`（Task 1）、`Relics.JOKER_TRANSFORM_ID`/`GUARD_SHIELD_ID`/`def_by_id()`、`UITheme.color()`。
- Produces: `RelicBar`（`extends Control`），`const SLOT_COUNT := 3`、`const SLOT_SIZE := 32`、`const SLOT_GAP := 4`、`func setup(state: Dictionary, seat: int) -> void`（填充 3 槽 + `n/3` + tooltip，`size` 同步为最小尺寸）。Task 3 依赖 `bar.size` 定位。

- [ ] **Step 1: 场景实体**

`scenes/ui/relic_bar.tscn`：
```
[gd_scene format=3 uid="uid://crelicbar0001"]

[ext_resource type="Script" path="res://scripts/ui/relic_bar.gd" id="1"]

[node name="RelicBar" type="Control"]
script = ExtResource("1")
layout_mode = 3
custom_minimum_size = Vector2(32, 0)
mouse_filter = 2
```

- [ ] **Step 2: 脚本**

`scripts/ui/relic_bar.gd`：
```gdscript
class_name RelicBar
extends Control
## 遗物物品栏：3 槽 + n/3 计数，程序化图标，Hover Tooltip（名称+描述+状态）。
## 纯展示：数据全部来自快照公开字段（run.relics / run.relic_owners / players[].protected_slot）。

const SLOT_COUNT := 3
const SLOT_SIZE := 32
const SLOT_GAP := 4
const COUNT_H := 16

## 填充本玩家（seat）的遗物栏。护盾按生效中（protected_slot>=0）显示，Joker 按持有显示。
func setup(state: Dictionary, seat: int) -> void:
	for child in get_children():
		child.queue_free()
	var items: Array = _collect_items(state, seat)
	var y := 0.0
	for i in SLOT_COUNT:
		var icon := RelicIcon.new()
		icon.custom_minimum_size = Vector2(SLOT_SIZE, SLOT_SIZE)
		icon.size = Vector2(SLOT_SIZE, SLOT_SIZE)
		icon.position = Vector2(0, y)
		icon.mouse_filter = Control.MOUSE_FILTER_STOP
		if i < items.size():
			var it: Dictionary = items[i]
			icon.setup(int(it.kind), it.color)
			icon.tooltip_text = "%s\n%s\n%s" % [it.name, it.desc, it.status]
		else:
			icon.setup(RelicIcon.Kind.EMPTY, UITheme.color("border_strong"))
			icon.tooltip_text = "空遗物栏位"
		add_child(icon)
		y += SLOT_SIZE + SLOT_GAP
	var count := Label.new()
	count.text = "%d/%d" % [items.size(), SLOT_COUNT]
	count.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	count.position = Vector2(0, y)
	count.custom_minimum_size = Vector2(SLOT_SIZE, COUNT_H)
	count.add_theme_font_size_override("font_size", 11)
	count.add_theme_color_override("font_color", UITheme.color("text_secondary"))
	add_child(count)
	custom_minimum_size = Vector2(SLOT_SIZE, y + COUNT_H)
	size = custom_minimum_size

## 收集该玩家当前应显示的遗物（Joker 持有一件；护盾生效一件；未来遗物依序追加）。
func _collect_items(state: Dictionary, seat: int) -> Array:
	var run: Dictionary = state.get("run", {})
	var relics: Dictionary = run.get("relics", {})
	var owners: Dictionary = run.get("relic_owners", {})
	var items: Array = []
	if str(owners.get(Relics.JOKER_TRANSFORM_ID, -1)) == str(seat) and relics.has(Relics.JOKER_TRANSFORM_ID):
		var def: Dictionary = relics[Relics.JOKER_TRANSFORM_ID]
		items.append({"kind": RelicIcon.Kind.JOKER, "color": UITheme.color("accent"),
			"name": str(def.get("name", "Joker 变换")), "desc": str(def.get("desc", "")),
			"status": "状态：本局可用"})
	var shield_slot := -1
	for p in state.get("players", []):
		if int(p.id) == seat:
			shield_slot = int(p.get("protected_slot", -1))
			break
	if shield_slot >= 0:
		var def: Dictionary = Relics.def_by_id(Relics.GUARD_SHIELD_ID)
		items.append({"kind": RelicIcon.Kind.SHIELD, "color": UITheme.color("relic_shield"),
			"name": str(def.get("name", "防守护盾")), "desc": str(def.get("desc", "")),
			"status": "状态：本局保护第 %d 格" % shield_slot})
	return items
```

- [ ] **Step 3: 编译检查 + 回归**

Run:
```bash
tools/run_all_tests.sh --quick
```
Expected: 编译无错；全绿。

- [ ] **Step 4: 提交**

```bash
git add scenes/ui/relic_bar.tscn scripts/ui/relic_bar.gd
git commit -m "feat: 遗物物品栏 RelicBar——3槽+n/3+程序化图标+Hover Tooltip(名称/描述/状态)"
```
若生成 `.uid`，一并 add。

---

### Task 3: game_view 集成（护盾紫光 + RelicBar 挂载）

**Files:**
- Modify: `scripts/ui/game_view.gd`
  - `_render_card_slot`：护盾紫色描边光晕 + ◈ 角标改紫（现 `UITheme.color("accent")` → `relic_shield`）。
  - `_render_player_section`：为每个玩家区域挂载/复用 RelicBar 并定位到手牌左侧。
- Test: `tools/run_all_tests.sh --quick`（编译检查 + 回归）

**Interfaces:**
- Consumes: `RelicBar`（Task 2）、`UITheme.color("relic_shield")`（Task 1）、`main.latest_state`。
- Produces: 无对外新接口（纯渲染）。

- [ ] **Step 1: 护盾紫色描边 + ◈ 改紫**

`game_view.gd` 现有 `_render_card_slot` 中的护盾标记块（`main._highlight(...)` 之后的 `if bool(slot.get("protected", false)) and card_button is CardView:` 块）整体替换为：
```gdscript
	# 防守护盾标记：受保护槽位常驻紫色描边光晕 + ◈ 紫色角标（不干扰点击）
	if bool(slot.get("protected", false)) and card_button is CardView:
		var shield_border := Panel.new()
		shield_border.name = "ShieldBorder"
		shield_border.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
		shield_border.mouse_filter = Control.MOUSE_FILTER_IGNORE
		var st := StyleBoxFlat.new()
		st.bg_color = Color(0, 0, 0, 0)
		st.set_corner_radius_all(6)
		st.border_color = UITheme.color("relic_shield")
		st.set_border_width_all(3)
		shield_border.add_theme_stylebox_override("panel", st)
		card_button.add_child(shield_border)
		var shield := Label.new()
		shield.text = "◈"
		shield.set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
		shield.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
		shield.vertical_alignment = VERTICAL_ALIGNMENT_CENTER
		shield.add_theme_font_size_override("font_size", 22)
		shield.add_theme_color_override("font_color", UITheme.color("relic_shield"))
		shield.mouse_filter = Control.MOUSE_FILTER_IGNORE
		card_button.add_child(shield)
```

- [ ] **Step 2: RelicBar 挂载 + 定位**

`game_view.gd` 顶部常量区加：
```gdscript
const RelicBarScript := preload("res://scenes/ui/relic_bar.tscn")
```

`_render_player_section` 末尾（`if is_me: name_label.text = "▼ " + ...` 之后、函数结尾前）加：
```gdscript
	_render_relic_bar(area, player, hand)
```

新增方法（放在 `_render_player_section` 之后）：
```gdscript
## 玩家区域左侧遗物物品栏：复用单实例，刷新内容并定位到手牌左侧、垂直居中。
func _render_relic_bar(area: Control, player: Dictionary, hand: GridContainer) -> void:
	var bar := area.get_node_or_null("RelicBar")
	if bar == null:
		bar = RelicBarScript.instantiate()
		bar.name = "RelicBar"
		bar.mouse_filter = Control.MOUSE_FILTER_PASS
		area.add_child(bar)
	bar.setup(main.latest_state, int(player.id))
	var hand_rect: Rect2 = hand.get_global_rect()
	var target := hand_rect.position + Vector2(-bar.size.x - 8, (hand_rect.size.y - bar.size.y) / 2.0)
	bar.global_position = target
```

> 注：`hand` 变量即 `_render_player_section` 中的 `area.get_node("VBox/HandCenter/HandGrid")`（`GridContainer`），已在函数内定义，直接传入。

- [ ] **Step 3: 编译检查 + 回归**

Run:
```bash
tools/run_all_tests.sh --quick
```
Expected: 编译无错；12/12 全绿。

- [ ] **Step 4: 提交**

```bash
git add scripts/ui/game_view.gd
git commit -m "feat: 护盾受保护格常驻紫色描边光晕+◈紫角标；每玩家区域左侧挂载遗物物品栏(RelicBar)"
```

---

### Task 4: 商店遗物图标复用（一致性）

**Files:**
- Modify: `scripts/ui/shop_panel.gd`（`_make_relic_slot` 的文本符号图标改为 RelicIcon 着色）
- Test: `tools/run_all_tests.sh --quick`

**Interfaces:**
- Consumes: `RelicIcon`（Task 1）、`Relics.pool()`、`UITheme.color("relic_shield")`/`("accent")`。

- [ ] **Step 1: 替换文本图标**

`shop_panel.gd`：删除 `const RELIC_ICONS := ["◈", "◇"]`，`_make_relic_slot` 改为在按钮上方叠加 RelicIcon（与物品栏同视觉语言）。`_fill_relics` 传 `relic_id` 决定颜色：
```gdscript
func _fill_relics() -> void:
	var row: HBoxContainer = get_node("Center/Panel/VBox/RelicsRow")
	var pool: Array = Relics.pool()
	for i in mini(RELIC_PRICES.size(), pool.size()):
		var relic: Dictionary = pool[i]
		row.add_child(_make_relic_slot(str(relic.get("id", "")), str(relic.get("name", "")), RELIC_PRICES[i]))
```
`_make_relic_slot` 签名改为 `(relic_id: String, name: String, price: int)`，按钮 `text` 去掉图标前缀，并加图标子节点：
```gdscript
func _make_relic_slot(relic_id: String, name: String, price: int) -> Control:
	var box := VBoxContainer.new()
	box.add_theme_constant_override("separation", 6)
	var btn := Button.new()
	btn.custom_minimum_size = Vector2(150, 118)
	btn.toggle_mode = true
	btn.text = name
	btn.alignment = HORIZONTAL_ALIGNMENT_CENTER
	btn.add_theme_font_size_override("font_size", 16)
	btn.add_theme_color_override("font_color", UITheme.color("text_primary"))
	var style := StyleBoxFlat.new()
	style.bg_color = UITheme.color("bg_elevated")
	style.bg_color.a = 0.85
	style.set_corner_radius_all(10)
	style.set_content_margin_all(10)
	style.border_color = UITheme.color("border")
	style.set_border_width_all(1)
	btn.add_theme_stylebox_override("normal", style)
	var hover: StyleBoxFlat = style.duplicate()
	hover.border_color = UITheme.color("accent")
	btn.add_theme_stylebox_override("hover", hover)
	var down: StyleBoxFlat = style.duplicate()
	down.bg_color.a = 1.0
	down.border_color = UITheme.color("success")
	btn.add_theme_stylebox_override("pressed", down)
	box.add_child(btn)
	var icon := RelicIcon.new()
	icon.custom_minimum_size = Vector2(36, 36)
	icon.size = Vector2(36, 36)
	icon.setup(RelicIcon.Kind.SHIELD if relic_id == Relics.GUARD_SHIELD_ID else RelicIcon.Kind.JOKER,
		UITheme.color("relic_shield") if relic_id == Relics.GUARD_SHIELD_ID else UITheme.color("accent"))
	icon.mouse_filter = Control.MOUSE_FILTER_IGNORE
	btn.add_child(icon)
	var price_lbl := Label.new()
	price_lbl.text = "$%d" % price
	price_lbl.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	price_lbl.add_theme_font_size_override("font_size", 18)
	price_lbl.add_theme_color_override("font_color", UITheme.color("accent"))
	box.add_child(price_lbl)
	return box
```
> 说明：`icon` 是 `btn` 子节点，需绝对定位到按钮上部（`btn` 为普通 Button，子节点默认左上角）。为简化，不精确排版，图标置于按钮顶部中央区域即可（可设置 `icon.position`；若无需精确，保留文本图标也可，本任务为可选一致性改动）。

- [ ] **Step 2: 编译检查 + 回归**

Run:
```bash
tools/run_all_tests.sh --quick
```
Expected: 编译无错；全绿。

- [ ] **Step 3: 提交**

```bash
git add scripts/ui/shop_panel.gd
git commit -m "feat: 商店遗物图标复用程序化图标(盾紫/星金)，与物品栏视觉一致"
```

---

### Task 5: 文档同步 + 全量回归 + 提交

**Files:**
- Modify: `docs/AI_AGENT_交接说明.md`（§2 遗物摘要补"UI：物品栏+护盾紫光"；§3 不变量 10 补卡面光晕/物品栏纯展示说明）
- Modify: `docs/功能实现文档.md`（遗物效果 v1 条目补客户端 UI 文件）
- Modify: `docs/修改日志.md`（追加记录，不提交，gitignored）

**Interfaces:** 无。

- [ ] **Step 1: 文档更新**

`AI_AGENT_交接说明.md` §2「遗物效果 v1（已完成）」条目末尾补：
```
客户端 UI：护盾受保护格常驻紫色描边光晕 + ◈ 紫角标（`game_view._render_card_slot`）；每玩家区域左侧遗物物品栏（`RelicBar`：3 槽 + n/3 + 程序化图标 + Hover Tooltip，数据来自快照公开字段，纯展示无协议改动）。
```
`功能实现文档.md` 遗物效果 v1 条目落点列表补：`scripts/ui/relic_icon.gd`（程序化盾/星图标）、`scripts/ui/relic_bar.gd` + `scenes/ui/relic_bar.tscn`（物品栏）。

- [ ] **Step 2: 全量回归**

Run:
```bash
tools/run_all_tests.sh --quick
```
Expected: 编译检查无脚本错误；12/12 全绿（protocol/swap/duel/reconnect/kongbaya/settlement/series/economy/shop/relics/hint）。

- [ ] **Step 3: 追加修改日志（不提交）**

`docs/修改日志.md` 末尾追加本次 request→实现记录（gitignore，不纳入提交）。

- [ ] **Step 4: 提交 + 推送**

```bash
git add docs/AI_AGENT_交接说明.md docs/功能实现文档.md
git commit -m "doc: 遗物UI同步——交接说明/功能文档补物品栏+护盾紫光(纯展示无协议改动)"
git push origin feature/store
```

---

## Self-Review

**Spec 覆盖核对：**
- 2.1 护盾卡面紫光描边 + ◈ 角标 → Task 3 Step 1（`relic_shield` 边框 Panel + ◈ 改紫）。
- 2.2 物品栏（每玩家左侧/3槽/n3/程序化图标/Hover Tooltip/透明背景）→ Task 2（RelicBar）+ Task 3 Step 2（挂载定位）。
- 2.3 主题 token → Task 1 Step 1。
- 3 组件文件表 → Task 1（relic_icon）/Task 2（relic_bar+tscn）/Task 3（game_view）/Task 1（ui_theme）/Task 4（shop_panel 可选）。
- 5 测试与验证 → 每任务 `--quick` 回归 + Task 5 文档。

**占位符扫描：** 无 TBD/TODO；每步给出具体 GDScript/命令。Task 4 为可选一致性改动，已注明简化处理。

**类型一致性：** `RelicIcon.Kind.{EMPTY,SHIELD,JOKER}`、`RelicIcon.setup(kind,color)`、`RelicBar.setup(state,seat)`、`RelicBar.SLOT_COUNT/SLOT_SIZE/SLOT_GAP`、`UITheme.color("relic_shield")` 在各任务间一致；`game_view` 用 `bar.size`/`bar.global_position` 定位，`_render_player_section` 传入 `hand`（`GridContainer`）到 `_render_relic_bar`。

**范围：** 仅遗物 UI 展示层（物品栏 + 护盾紫光 + 商店图标一致性）。不涉及服务器、协议、快照、贴牌时序；物品栏 3 槽为未来遗物预留，不做拖拽/交互。