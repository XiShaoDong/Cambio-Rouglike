# KONG 货币经济（Milestone 2）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入**押注经济**：起始 100 货币 + 1 灵魂币（health）；每局开始押注（入场 20，+5 递增，上限 100）；结算时第一名 2× 返还并拿走垫底者押注、其他非最后 1.5× 返还、最后一名全失、差额系统补足；货币 < 20 自动 all-in（输→出局）。

**Architecture:** 全服务器权威。新增 `Phase.BET`（值 9，插在开局记忆确认之后、正式回合之前）；每局押注存 `bets[seat] = {amount, all_in}`；`_finish_game`/`_finish_game_over_hand` 改走 `_settle_economy()`（返回 `gains`，存 `last_result.economy`）；灵魂币复用 `health`（默认改 1），`health→0` 仅由 all-in 垫底触发。客户端：押注面板做成 `.tscn` 场景实体（W/S 调整 + Enter/按钮确认）；玩家区域显示货币；结算页显示本局货币变化。`bets` 为空时经济结算为 no-op，保证既有测试不回归。

**Tech Stack:** Godot 4.6 / GDScript / ENet。headless 单测（`tests/verify_economy.tscn`）。

**Spec:** `docs/superpowers/specs/2026-09-07-series-shop-relics-design.md` §4；用户确认规则（2026-09-08）：系统补足、第一 2× + 垫底者押注、其他 1.5×、起始 100 / 入场 20 / +5 递增 / 上限 100、货币<20 自动 all-in、并列垫底同失、并列第一平分。

## Global Constraints

- 保持 `GameState` 服务器权威、私有快照、贴牌服务器时序三条架构不变量不变。
- `bets` 为空时经济结算不得改变任何状态（既有测试 `verify_series` 等多次调用 `_finish_game` 无押注，必须保持全绿）。
- `health` = 灵魂币（1=活 0=出局），`_is_alive` 语义不变；`health→0` 仅由 all-in 垫底触发（Kong 首回合失败按"最后一名"处理）。
- 新 UI 优先 `.tscn` 场景实体（押注面板 `scenes/ui/bet_panel.tscn`）。
- 改协议前更新 `docs/网络协议_V1.md`；改规则前更新 `docs/KONG_开发文档.md`。
- 每次改动跑回归 `tools/run_all_tests.sh --quick`；提交 `feat/fix/doc` 分类。

---

### Task 1: 经济结算核心（常量 + 灵魂币 + `_finish_game` 经济化）

**Files:**
- Modify: `scripts/core/game_rules.gd`
- Modify: `scripts/core/game_state.gd`（`bets` 字段、`_finish_game`、`_finish_game_over_hand`、新增 `_settle_economy`）
- Create: `tests/verify_economy.gd`、`tests/verify_economy.tscn`

**Interfaces:**
- Consumes: 现有 `_calculate_ranking`（存活序）、`_same_score`、`_is_alive`/`_alive_order`、`_series_finished`。
- Produces: `KongRules.START_CURRENCY=100/MIN_BET=20/MAX_BET=100/BET_STEP=5`；`GameState.bets: Dictionary`；`GameState._settle_economy(winners: Array, last_group: Array) -> Dictionary`（返回 `{seat: net_gain}`）；`_finish_game` 不再扣通用 health，`last_result` 新增 `economy`、`penalized`=本局 all-in 垫底出局者。

- [ ] **Step 1: 写失败测试**

`tests/verify_economy.gd`（结算数学直接驱动，不依赖 BET 阶段）：

```gdscript
extends Node
## headless 单元测试：货币经济（Milestone 2）

var failures := 0
var checks := 0
var _rejections := 0

func _ready() -> void:
	await _test_defaults()
	await _test_settlement()
	await _test_all_in_loss()
	await _test_all_in_win()
	await _test_tie()
	await _test_kong_first_turn()
	await _test_over_hand()
	var status: String = " (FAILURES!)" if failures > 0 else ""
	print("=== ECONOMY RESULT: %d/%d passed%s ===" % [checks - failures, checks, status])
	get_tree().quit(1 if failures > 0 else 0)

func _check(name: String, ok: bool) -> void:
	checks += 1
	if ok:
		print("[PASS] " + name)
	else:
		failures += 1
		printerr("[FAIL] " + name)

## 开局 3 人；seat0 最低分(胜)、seat1 中、seat2 最高分(垫底)。
func _open_with_hands(bets: Dictionary) -> void:
	GameState._reset_match()
	GameState._add_player(1, "A")
	GameState._add_player(2, "B")
	GameState._add_player(3, "C")
	for seat in [0, 1, 2]:
		var vals: Array = [3, 4] if seat == 0 else ([5, 6] if seat == 1 else [7, 8])
		GameState.players[seat].cards = []
		for i in vals.size():
			var cid := "t%d_%d" % [seat, i]
			GameState.cards[cid] = {"id": cid, "rank": str(vals[i]), "suit": "♠", "value": vals[i]}
			GameState.players[seat].cards.append(cid)
	GameState.bets = bets

func _test_defaults() -> void:
	GameState._reset_match()
	GameState._add_player(1, "A")
	_check("起始货币=100", int(GameState.players[0].currency) == KongRules.START_CURRENCY)
	_check("灵魂币 health=1", int(GameState.players[0].health) == 1)
	_check("押注常量 20/5/100", KongRules.MIN_BET == 20 and KongRules.BET_STEP == 5 and KongRules.MAX_BET == 100)

func _test_settlement() -> void:
	_open_with_hands({0: {"amount": 20, "all_in": false}, 1: {"amount": 20, "all_in": false}, 2: {"amount": 20, "all_in": false}})
	GameState._finish_game()
	_check("胜者 2×+垫底者押注", int(GameState.players[0].currency) == 140)  # 100-20+60
	_check("安全玩家 1.5×", int(GameState.players[1].currency) == 110)      # 100-20+30
	_check("垫底者全失", int(GameState.players[2].currency) == 80)          # 100-20+0
	_check("胜场+1", int(GameState.players[0].wins) == 1)
	_check("economy gains 记录", int(GameState.last_result.economy[0]) == 40 and int(GameState.last_result.economy[1]) == 10 and int(GameState.last_result.economy[2]) == -20)
	_check("无人出局(无 all-in)", GameState.players[0].health == 1 and GameState.players[1].health == 1 and GameState.players[2].health == 1)

func _test_all_in_loss() -> void:
	_open_with_hands({0: {"amount": 20, "all_in": false}, 1: {"amount": 20, "all_in": false}, 2: {"amount": 10, "all_in": true}})
	GameState.players[2].currency = 10
	GameState._finish_game()
	_check("all-in 垫底出局", GameState.players[2].health == 0)
	_check("all-in 垫底货币=0", int(GameState.players[2].currency) == 0)
	_check("penalized 记录出局者", (GameState.last_result.penalized as Array).has(2))

func _test_all_in_win() -> void:
	_open_with_hands({0: {"amount": 10, "all_in": true}, 1: {"amount": 20, "all_in": false}, 2: {"amount": 20, "all_in": false}})
	GameState.players[0].currency = 10
	GameState._finish_game()
	_check("all-in 胜者保留灵魂币", GameState.players[0].health == 1)
	_check("all-in 胜者货币恢复", int(GameState.players[0].currency) > 0)

func _test_tie() -> void:
	# 并列第一：seat0/seat1 同分，平分 2×+垫底者押注；并列垫底 seat2/seat3 同失
	GameState._reset_match()
	GameState._add_player(1, "A")
	GameState._add_player(2, "B")
	GameState._add_player(3, "C")
	GameState._add_player(4, "D")
	for seat in [0, 1, 2, 3]:
		var vals: Array = [3, 3] if seat <= 1 else [9, 9]
		GameState.players[seat].cards = []
		for i in vals.size():
			var cid := "u%d_%d" % [seat, i]
			GameState.cards[cid] = {"id": cid, "rank": str(vals[i]), "suit": "♠", "value": vals[i]}
			GameState.players[seat].cards.append(cid)
	GameState.bets = {0: {"amount": 20, "all_in": false}, 1: {"amount": 20, "all_in": false}, 2: {"amount": 20, "all_in": false}, 3: {"amount": 20, "all_in": false}}
	GameState._finish_game()
	# 奖池给胜者组：2×20+2×20 + 垫底者押注(40) = 120，2 人平分 60
	_check("并列第一各拿60", int(GameState.players[0].currency) == 140 and int(GameState.players[1].currency) == 140)
	_check("并列垫底同失", int(GameState.players[2].currency) == 80 and int(GameState.players[3].currency) == 80)

func _test_kong_first_turn() -> void:
	_open_with_hands({0: {"amount": 20, "all_in": false}, 1: {"amount": 20, "all_in": false}, 2: {"amount": 20, "all_in": false}})
	GameState.kong_called_first_turn = true
	GameState.kong_caller = 1  # 安全玩家 seat1 首回合喊但非最低分 → 按最后一名处理
	GameState._finish_game()
	_check("Kong 首回合失败者按垫底失去押注", int(GameState.players[1].currency) == 80)
	_check("Kong 首回合不影响真正的垫底者", int(GameState.players[2].currency) == 110)  # seat2 变安全 1.5×
	_check("胜者拿到 Kong 失败者押注", int(GameState.players[0].currency) == 140)

func _test_over_hand() -> void:
	GameState._reset_match()
	GameState._add_player(1, "A")
	GameState._add_player(2, "B")
	GameState._add_player(3, "C")
	GameState.players[2].health = 0
	GameState.bets = {0: {"amount": 20, "all_in": false}, 1: {"amount": 20, "all_in": false}}
	GameState._server_start_match(0)
	for _i in 3:
		GameState.players[0].cards.append(GameState._draw_from_deck())
	GameState._finish_game_over_hand(0)
	_check("超限玩家按垫底失去押注", int(GameState.players[0].currency) == 80)
	_check("超限结算排除出局者", GameState.last_result.ranking.size() == 1)
```

`tests/verify_economy.tscn`：

```
[gd_scene format=3 uid="uid://beconomy000001"]

[ext_resource type="Script" path="res://tests/verify_economy.gd" id="1"]

[node name="VerifyEconomy" type="Node"]
script = ExtResource("1")
```

- [ ] **Step 2: 运行确认失败**

Run: `/Applications/Godot.app/Contents/MacOS/Godot --headless --path . res://tests/verify_economy.tscn`
Expected: FAIL（`_settle_economy` 未定义、常量不符、`_finish_game` 仍扣 health）。

- [ ] **Step 3: 实现**

`game_rules.gd` 常量区追加并修改：

```gdscript
const START_CURRENCY := 100
const MIN_BET := 20
const MAX_BET := 100
const BET_STEP := 5
const SAFE_RETURN := 1.5   # 非最后玩家返还倍数
const WINNER_RETURN := 2.0 # 第一名返还倍数
```
`new_default_run()` 的 `"health": 3` 改为 `"health": 1`（灵魂币=1 条命）。

`game_state.gd` —— 成员区（`match_number` 附近）追加：

```gdscript
var bets: Dictionary = {}  # key: seat；value: {amount:int, all_in:bool}
```

新增经济结算函数（放 `_finish_game` 之前）：

```gdscript
## 押注经济结算：押注时已扣货币，此处按名次补发。
## winners=最低分组；last_group=最高分组（含 Kong 首回合失败者/超限失败者）。
## 第一名(组) 2×返还+垫底者押注，其余非最后 1.5×返还，最后一名 0，差额系统补足。
## 返回 gains: {seat: net_gain}；bets 为空时 no-op。
func _settle_economy(winners: Array, last_group: Array) -> Dictionary:
	var gains: Dictionary = {}
	if bets.is_empty():
		return gains
	var last_pot := 0
	for seat in last_group:
		last_pot += int(bets.get(int(seat), {}).get("amount", 0))
	var winner_pot := last_pot
	for seat in winners:
		winner_pot += int(WINNER_RETURN * float(bets.get(int(seat), {}).get("amount", 0)))
	var per_winner := int(floor(winner_pot / float(maxi(1, winners.size()))))
	for seat in winners:
		var bet := int(bets.get(int(seat), {}).get("amount", 0))
		players[seat].currency = int(players[seat].currency) + per_winner
		gains[int(seat)] = per_winner - bet
	for seat in _alive_order():
		if int(seat) in winners or int(seat) in last_group:
			continue
		var bet := int(bets.get(int(seat), {}).get("amount", 0))
		var ret := int(floor(SAFE_RETURN * float(bet)))
		players[seat].currency = int(players[seat].currency) + ret
		gains[int(seat)] = ret - bet
	for seat in last_group:
		gains[int(seat)] = -int(bets.get(int(seat), {}).get("amount", 0))
	return gains
```

`_finish_game`（963-976 段）——用 `ranking.size()` 判定全员同分、删通用扣血、接入经济结算：

```gdscript
	var ranking := _calculate_ranking()
	var winners: Array[int] = []
	var losers: Array[int] = []
	if not ranking.is_empty():
		for entry in ranking:
			if _same_score(entry, ranking[0]): winners.append(int(entry.id))
			if _same_score(entry, ranking[ranking.size() - 1]): losers.append(int(entry.id))
	if winners.size() == ranking.size():
		losers.clear()
	if kong_called_first_turn and kong_caller not in winners:
		losers = [kong_caller]
	var economy: Dictionary = _settle_economy(winners, losers)
	var penalized: Array = []
	for seat in losers:
		if bool(bets.get(int(seat), {}).get("all_in", false)):
			players[seat].health = 0
			penalized.append(int(seat))
	last_result = {"ranking": ranking, "winners": winners, "penalized": penalized,
		"first_turn_kong": kong_called_first_turn, "economy": economy}
```

`_finish_game_over_hand`（994-1023 段）——删 `players[failed_seat].health = max(0, ...-1)`，改经济结算：

```gdscript
	var others: Array[int] = []
	for seat in _alive_order():
		if int(seat) != failed_seat:
			others.append(int(seat))
	var ranking := ScoreSystem.calculate_ranking(players, cards, others)
	var winners: Array[int] = []
	if not ranking.is_empty():
		for entry in ranking:
			if _same_score(entry, ranking[0]):
				winners.append(int(entry.id))
	var economy: Dictionary = _settle_economy(winners, [failed_seat])
	var penalized: Array = []
	if bool(bets.get(failed_seat, {}).get("all_in", false)):
		players[failed_seat].health = 0
		penalized.append(failed_seat)
	last_result = {
		"reason": "%s 手牌超过 %d 张，判定失败。" % [failed_name, KongRules.MAX_HAND_CARDS],
		"ranking": ranking, "winners": winners, "penalized": penalized,
		"failed_hand": failed_seat, "economy": economy,
	}
```

- [ ] **Step 4: 运行确认通过**

Run: `/Applications/Godot.app/Contents/MacOS/Godot --headless --path . res://tests/verify_economy.tscn`
Expected: PASS，12/12。

- [ ] **Step 5: 回归 + 提交**

Run: `tools/run_all_tests.sh --quick`
Expected: 全绿（既有套件不回归；`_finish_game` 无 bets → 经济 no-op）。

```bash
git add scripts/core/game_rules.gd scripts/core/game_state.gd tests/verify_economy.gd tests/verify_economy.tscn
git commit -m "feat: 押注经济结算核心——胜者2x+垫底者押注/安全1.5x/垫底全失/系统补足，灵魂币health=1"
```
若 Godot 生成 `.uid`，一并 `git add`。

---

### Task 2: BET 押注阶段（Phase 9 + 押注 RPC）

**Files:**
- Modify: `scripts/core/game_state.gd`（Phase 枚举、`_server_initial_ready`、`request_bet`/`server_bet`/`_server_bet`/`_commit_bet`、`_is_suspended`、`_deal_new_match`/`_reset_match` 清 `bets`）
- Modify: `scripts/core/hidden_info.gd`（`_phase_name`、快照 `bet_ready`）
- Modify: `tests/verify_economy.gd`（追加 `_test_bet_phase`、`_test_bet_all_in`）

**Interfaces:**
- Consumes: Task 1 的 `bets`/`_settle_economy`。
- Produces: `Phase.BET`（值 9）；`GameState.request_bet(amount: int)`、`server_bet(amount)`（RPC）、`_server_bet(sender, amount)`、`_commit_bet(seat, amount, all_in)`；`_start_bet_phase()`；`RejectCode.INVALID_AMOUNT`；快照 `bet_ready`（已押注 seat 列表）、`_player_snapshot.currency`。

- [ ] **Step 1: 写失败测试**

`_ready` 中 `await _test_defaults()` 之后追加 `await _test_bet_phase()`、`await _test_bet_all_in()`：

```gdscript
func _test_bet_phase() -> void:
	GameState.command_rejected.connect(func(_c: int, _m: String) -> void: _rejections += 1)
	_rejections = 0
	GameState._reset_match()
	GameState._add_player(1, "A")
	GameState._add_player(2, "B")
	GameState._add_player(3, "C")
	GameState._server_start_match(0)
	GameState._server_initial_ready(0)
	GameState._server_initial_ready(1)
	GameState._server_initial_ready(2)
	_check("开局确认后进入押注阶段", GameState.phase == GameState.Phase.BET)
	GameState._server_bet(0, 15)
	_check("低于入场被拒", _rejections == 1 and not GameState.bets.has(0))
	GameState._server_bet(0, 22)
	_check("非5倍数被拒", _rejections == 2)
	GameState._server_bet(0, 500)
	_check("超上限被拒", _rejections == 3)
	GameState._server_bet(0, 20)
	_check("正常押注生效并扣货币", GameState.bets.has(0) and int(GameState.players[0].currency) == 80)
	GameState._server_bet(0, 20)
	_check("重复押注被拒", _rejections == 4 and int(GameState.players[0].currency) == 80)
	GameState._server_bet(1, 20)
	_check("未全员押注不推进", GameState.phase == GameState.Phase.BET)
	GameState._server_bet(2, 20)
	_check("全员押注后进入抽牌", GameState.phase == GameState.Phase.TURN_DRAW and GameState.current_player_id == 0)
	_check("快照含 bet_ready", (GameState._snapshot_for(0).get("bet_ready", []) as Array).size() == 0)  # BET 已结束

func _test_bet_all_in() -> void:
	GameState._reset_match()
	GameState._add_player(1, "A")
	GameState._add_player(2, "B")
	GameState.players[1].currency = 10  # seat1 押不起入场
	GameState._server_start_match(0)
	GameState._server_initial_ready(0)
	GameState._server_initial_ready(1)
	GameState._server_bet(0, 20)
	GameState._server_bet(1, 0)  # all-in：全货币+灵魂币
	_check("all-in 押注记录全货币", GameState.bets.has(1) and int(GameState.bets[1].amount) == 10 and bool(GameState.bets[1].all_in))
	_check("all-in 后货币归零", int(GameState.players[1].currency) == 0)
	_check("双方押完进入抽牌", GameState.phase == GameState.Phase.TURN_DRAW)
```

- [ ] **Step 2: 运行确认失败**

Expected: FAIL（`Phase.BET` 不存在、`_server_bet` 未定义）。

- [ ] **Step 3: 实现**

`game_state.gd` Phase 枚举末尾追加：

```gdscript
enum Phase { LOBBY, INITIAL_PEEK, TURN_DRAW, TURN_DECISION, Q_DECISION, SLAP_WINDOW, SLAP_EXCHANGE, GAME_OVER, SLAP_DUEL, BET }
```

`RejectCode` 追加 `INVALID_AMOUNT`，`_reject_code_message` 加：

```gdscript
		RejectCode.INVALID_AMOUNT: return "押注金额不合法（入场 %d，+%d 递增，上限 %d）。" % [KongRules.MIN_BET, KongRules.BET_STEP, KongRules.MAX_BET]
```

`_server_initial_ready` 全员确认段（原 `current_player_id = _alive_order()[0]; phase = TURN_DRAW; ...`）改为：

```gdscript
	if initial_confirmed.size() < _alive_count():
		_broadcast_state()
		return
	_start_bet_phase()
```

新增：

```gdscript
## 进入押注阶段（开局记忆确认后、正式回合前）。
func _start_bet_phase() -> void:
	bets.clear()
	phase = Phase.BET
	_add_log("开始押注：入场 %d，+%d 递增。" % [KongRules.MIN_BET, KongRules.BET_STEP])
	_broadcast_state()

func request_bet(amount: int) -> void:
	if multiplayer.is_server():
		_server_bet(_peer_to_seat(1), amount)
	else:
		server_bet.rpc_id(1, amount)

@rpc("any_peer", "reliable")
func server_bet(amount: int) -> void:
	if multiplayer.is_server():
		_server_bet(_peer_to_seat(multiplayer.get_remote_sender_id()), amount)

func _server_bet(sender: int, amount: int) -> void:
	if phase != Phase.BET:
		_reject(sender, RejectCode.INVALID_PHASE)
		return
	if _guard_suspended(sender, ""):
		return
	if _guard_spectator(sender, ""):
		return
	if not players.has(sender) or bets.has(sender):
		_reject(sender, RejectCode.DUPLICATE_OR_EXPIRED_ACTION)
		return
	var currency: int = int(players[sender].currency)
	var all_in := false
	var bet_amount := amount
	if amount == 0:
		if currency >= KongRules.MIN_BET:
			_reject(sender, RejectCode.INVALID_AMOUNT)
			return
		all_in = true
		bet_amount = currency
	else:
		if amount < KongRules.MIN_BET or amount > KongRules.MAX_BET:
			_reject(sender, RejectCode.INVALID_AMOUNT)
			return
		if (amount - KongRules.MIN_BET) % KongRules.BET_STEP != 0:
			_reject(sender, RejectCode.INVALID_AMOUNT)
			return
		if amount > currency:
			_reject(sender, RejectCode.INVALID_AMOUNT)
			return
	_commit_bet(sender, bet_amount, all_in)

func _commit_bet(seat: int, amount: int, all_in := false) -> void:
	players[seat].currency = int(players[seat].currency) - amount
	bets[seat] = {"amount": amount, "all_in": all_in}
	_add_log("%s 押注 %d%s。" % [players[seat].name, amount, "（all-in）" if all_in else ""])
	if bets.size() >= _alive_count():
		current_player_id = _alive_order()[0]
		phase = Phase.TURN_DRAW
		_add_log("轮到 %s 行动。" % players[current_player_id].name)
		_broadcast_state()
	else:
		_broadcast_state()
```

`_is_suspended`：INITIAL_PEEK 分支改为覆盖 BET 且只算存活者：

```gdscript
	if phase == Phase.INITIAL_PEEK or phase == Phase.BET:
		for seat in _alive_order():
			if bool(players[seat].get("offline", false)):
				return true
		return false
```

`_deal_new_match` 与 `_reset_match` 各自 `bets.clear()`（`_deal_new_match` 中 `event_log.clear()` 附近）。

`hidden_info.gd`：`_phase_name` 加 `GameState.Phase.BET: return "押注"`；`snapshot_for` 顶层加 `"bet_ready": state.bets.keys() if phase == GameState.Phase.BET else []`；`_player_snapshot` 返回值加 `"currency": int(player.currency)`。

- [ ] **Step 4: 运行确认通过**

Run: `/Applications/Godot.app/Contents/MacOS/Godot --headless --path . res://tests/verify_economy.tscn`
Expected: PASS，累计 29/29（12 + 17）。跑 `verify_series` 确认 `_server_initial_ready` 改动（确认后进 BET 而非 TURN_DRAW）不破坏 series 测试——注意 `verify_series` 大量测试在 ready 后直接断言 `TURN_DRAW`，需改为 BET 语义：可跑 `verify_series` 看失败项，逐条把"就绪后 TURN_DRAW"断言改为"就绪后 BET"并补一次 `_server_bet` 推进（或用 `GameState._start_bet_phase()` 后直接设 `current_player_id` 的辅助）。**优先保证 `verify_economy` 全绿；`verify_series` 同步语义更新为"就绪→BET→（押注）→TURN_DRAW"。**

- [ ] **Step 5: 提交**

```bash
git add scripts/core/game_state.gd scripts/core/hidden_info.gd tests/verify_economy.gd
git commit -m "feat: 押注阶段 BET(Phase9)——开局确认后押注，request_bet/all-in/INVALID_AMOUNT，快照 bet_ready/currency"
```

---

### Task 3: 客户端押注面板（.tscn 场景实体）

**Files:**
- Create: `scenes/ui/bet_panel.tscn`
- Create: `scripts/ui/bet_panel.gd`
- Modify: `scripts/ui/main.gd`（`PHASE_BET`、`bet_panel` 变量、`_on_state_updated` 开/关、`_on_bet_confirm`、`_request_bet`）

**Interfaces:**
- Consumes: `GameState.request_bet(amount)`；快照 `viewer_id`/`currency`/`run.match_limit`/`match_number`/`bet_ready`。
- Produces: `class_name BetPanel extends Control`；`setup(state: Dictionary, on_confirm: Callable)`；W/S/上/下/Enter 键处理；`main.bet_panel`；`main._on_bet_confirm(amount)`。

- [ ] **Step 1: 场景实体**

`scenes/ui/bet_panel.tscn`：

```
[gd_scene format=3 uid="uid://cbetpanel0001"]

[ext_resource type="Script" path="res://scripts/ui/bet_panel.gd" id="1"]

[node name="BetPanel" type="Control"]
script = ExtResource("1")
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
mouse_filter = 0

[node name="Center" type="CenterContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
mouse_filter = 2

[node name="Panel" type="PanelContainer" parent="Center"]
custom_minimum_size = Vector2(360, 0)
layout_mode = 2

[node name="VBox" type="VBoxContainer" parent="Center/Panel"]
layout_mode = 2
theme_override_constants/separation = 10

[node name="Title" type="Label" parent="Center/Panel/VBox"]
layout_mode = 2
text = "押注"
horizontal_alignment = 1
theme_override_font_sizes/font_size = 20

[node name="Currency" type="Label" parent="Center/Panel/VBox"]
layout_mode = 2
text = "当前货币：-"
horizontal_alignment = 1

[node name="BetAmount" type="Label" parent="Center/Panel/VBox"]
layout_mode = 2
text = "押注：20"
horizontal_alignment = 1
theme_override_font_sizes/font_size = 28

[node name="Hint" type="Label" parent="Center/Panel/VBox"]
layout_mode = 2
text = "W/S 调整押注（+5 递增） · Enter 确认"
horizontal_alignment = 1
theme_override_colors/font_color = Color(0.75, 0.75, 0.75, 1)

[node name="Buttons" type="HBoxContainer" parent="Center/Panel/VBox"]
layout_mode = 2
alignment = 1

[node name="Minus" type="Button" parent="Center/Panel/VBox/Buttons"]
layout_mode = 2
text = "−（-5）"

[node name="Plus" type="Button" parent="Center/Panel/VBox/Buttons"]
layout_mode = 2
text = "+（+5）"

[node name="Confirm" type="Button" parent="Center/Panel/VBox"]
layout_mode = 2
text = "确认押注（20）"

[node name="Status" type="Label" parent="Center/Panel/VBox"]
layout_mode = 2
text = ""
horizontal_alignment = 1
theme_override_colors/font_color = Color(0.6, 0.6, 0.6, 1)
```

- [ ] **Step 2: 脚本**

`scripts/ui/bet_panel.gd`：

```gdscript
class_name BetPanel
extends Control
## 押注面板：W/S 调整押注额（入场 20，+5 递增，上限 100 或 ≤ 货币），Enter/按钮确认。
## 场景实体承载骨架；货币 < 入场时自动 all-in（amount=0）。

var _amount := KongRules.MIN_BET
var _currency := 0
var _all_in := false
var _on_confirm: Callable = Callable()

func setup(state: Dictionary, on_confirm: Callable) -> void:
	_on_confirm = on_confirm
	set_anchors_and_offsets_preset(Control.PRESET_FULL_RECT)
	_currency = 0
	for player in state.get("players", []):
		if int(player.id) == int(state.get("viewer_id", -1)):
			_currency = int(player.get("currency", 0))
			break
	var title: Label = get_node("Center/Panel/VBox/Title")
	var limit: int = int((state.get("run", {}) as Dictionary).get("match_limit", KongRules.DEFAULT_MATCH_LIMIT))
	title.text = "押注 · 第 %d / %d 局" % [int(state.get("match_number", 1)), limit]
	if _currency < KongRules.MIN_BET:
		_all_in = true
		_amount = _currency
		var bet_lbl: Label = get_node("Center/Panel/VBox/BetAmount")
		bet_lbl.text = "ALL-IN %d" % _amount
		var hint: Label = get_node("Center/Panel/VBox/Hint")
		hint.text = "货币不足入场，自动全押 + 灵魂币（输则出局）"
		var confirm: Button = get_node("Center/Panel/VBox/Confirm")
		confirm.text = "确认 ALL-IN"
		get_node("Center/Panel/VBox/Buttons").visible = false
	_refresh()

func _unhandled_input(event: InputEvent) -> void:
	if not visible:
		return
	if event is InputEventKey and event.pressed and not event.echo:
		if event.keycode == KEY_W or event.keycode == KEY_UP:
			_change_step(1)
			get_viewport().set_input_as_handled()
		elif event.keycode == KEY_S or event.keycode == KEY_DOWN:
			_change_step(-1)
			get_viewport().set_input_as_handled()
		elif event.keycode == KEY_ENTER or event.keycode == KEY_KP_ENTER:
			_confirm()
			get_viewport().set_input_as_handled()

func _refresh() -> void:
	if _all_in:
		return
	var cur: Label = get_node("Center/Panel/VBox/Currency")
	cur.text = "当前货币：%d" % _currency
	var bet_lbl: Label = get_node("Center/Panel/VBox/BetAmount")
	bet_lbl.text = "押注：%d" % _amount
	var confirm: Button = get_node("Center/Panel/VBox/Confirm")
	confirm.text = "确认押注（%d）" % _amount
	var status: Label = get_node("Center/Panel/VBox/Status")
	status.text = "等待其他玩家押注…"

func _change_step(dir: int) -> void:
	if _all_in:
		return
	var max_bet := mini(KongRules.MAX_BET, _currency)
	_amount = clampi(_amount + dir * KongRules.BET_STEP, KongRules.MIN_BET, max_bet)
	_refresh()

func _confirm() -> void:
	if _on_confirm.is_valid():
		_on_confirm.call(0 if _all_in else _amount)
```

按钮接线（在 `_ready` 或脚本内 connect）：`Minus`/`Plus`/`Confirm` 按钮 connect 到 `_change_step(-1)`/`_change_step(1)`/`_confirm`。在 `_ready` 中：

```gdscript
func _ready() -> void:
	get_node("Center/Panel/VBox/Buttons/Minus").pressed.connect(func() -> void: _change_step(-1))
	get_node("Center/Panel/VBox/Buttons/Plus").pressed.connect(func() -> void: _change_step(1))
	get_node("Center/Panel/VBox/Confirm").pressed.connect(_confirm)
```

- [ ] **Step 3: main.gd 接入**

```gdscript
const PHASE_BET := 9
const BetPanelScript := preload("res://scenes/ui/bet_panel.tscn")
var bet_panel: Control = null
```

`_on_state_updated` 中（GAME_OVER 处理附近）：

```gdscript
	if int(state.phase) == PHASE_BET:
		_open_bet_panel()
	else:
		_close_bet_panel()
```

新增：

```gdscript
## BET 阶段打开押注面板（幂等）。
func _open_bet_panel() -> void:
	if bet_panel != null and is_instance_valid(bet_panel):
		return
	var panel := BetPanelScript.instantiate()
	panel.name = "BetPanel"
	panel.z_index = 90
	add_child(panel)
	panel.setup(latest_state, _on_bet_confirm)
	bet_panel = panel

func _close_bet_panel() -> void:
	if bet_panel != null and is_instance_valid(bet_panel):
		bet_panel.queue_free()
	bet_panel = null

func _on_bet_confirm(amount: int) -> void:
	GameState.request_bet(amount)
```

- [ ] **Step 4: 验证**

Run:
```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . --quit-after 5
/Applications/Godot.app/Contents/MacOS/Godot --headless --path . res://tests/verify_economy.tscn
```
Expected: 编译无错；verify_economy 全绿。

- [ ] **Step 5: 提交**

```bash
git add scenes/ui/bet_panel.tscn scripts/ui/bet_panel.gd scripts/ui/main.gd
git commit -m "feat: 押注面板场景实体（W/S 调整 + Enter 确认 + 自动 all-in）"
```

---

### Task 4: 货币显示 + 结算经济显示

**Files:**
- Modify: `scripts/ui/game_view.gd`（玩家区域货币）
- Modify: `scenes/ui/settlement_page.tscn`（`EconomyLabel`）
- Modify: `scripts/ui/settlement_page.gd`（`setup` 接 economy，`_show_economy`）
- Modify: `scripts/ui/main.gd`（`_open_settlement` 传 economy）

**Interfaces:**
- Consumes: 快照 `players[].currency`、`result.economy`。
- Produces: 玩家名字行显示 `货币 N`；结算页显示"本局货币变化：A +40 · B +10 · C −20"。

- [ ] **Step 1: 玩家区域货币显示**

`game_view.gd` `_render_player_section` 名字标签（eliminated 后缀之后、▲/▼ 之前）追加：

```gdscript
	name_label.text = "%s%s%s%s%s" % [player.name, suffix, ready_mark, elim_suffix,
		"  ·  货币 %d" % int(player.get("currency", 0))]
```

- [ ] **Step 2: 结算页经济显示**

`settlement_page.tscn` 的 `StatusLabel` 之后追加：

```
[node name="EconomyLabel" type="Label" parent="Center/Panel/VBox"]
layout_mode = 2
visible = false
horizontal_alignment = 1
theme_override_font_sizes/font_size = 14
theme_override_colors/font_color = Color(0.96, 0.84, 0.48, 1)
```

`settlement_page.gd`：`setup` 追加参数 `economy := {}`；`_run_sequence` 末尾（`_show_series_summary` 之后、`_show_footer` 之前）调 `_show_economy(economy)`；新增：

```gdscript
func _show_economy(economy: Dictionary) -> void:
	if economy.is_empty():
		return
	var parts: Array = []
	for seat in economy:
		var gain: int = int(economy[seat])
		var name := ""
		for row in _rows:
			if int(row) == int(seat):
				name = str(_rows[row].name.text).replace("★ ", "").replace("▼ ", "").replace("▲ ", "")
				break
		parts.append("%s %+d" % [name, gain])
	var lbl: Label = get_node("Center/Panel/VBox/EconomyLabel")
	lbl.text = "本局货币：%s" % "　".join(parts)
	lbl.visible = true
```

`main.gd` `_open_settlement` 的 `page.setup(...)` 追加实参：

```gdscript
	var economy: Dictionary = (latest_state.get("result", {}) as Dictionary).get("economy", {})
	page.setup(model, Network.is_host, int(latest_state.get("match_number", 1)),
		_play_pending_winner, _request_next_match, GameState.request_abort_match,
		_on_settlement_flip, true, match_limit, series, economy)
```

- [ ] **Step 3: 验证**

Run: `tools/run_all_tests.sh --quick`；Expected: 全绿。

- [ ] **Step 4: 提交**

```bash
git add scripts/ui/game_view.gd scenes/ui/settlement_page.tscn scripts/ui/settlement_page.gd scripts/ui/main.gd
git commit -m "feat: 玩家货币显示 + 结算页本局货币变化"
```

---

### Task 5: 文档 + 回归接入 + 全量回归

**Files:**
- Modify: `docs/KONG_开发文档.md`、`docs/网络协议_V1.md`、`docs/功能实现文档.md`、`docs/AI_AGENT_交接说明.md`、`tools/run_all_tests.sh`

**Interfaces:** 无（纯文档 + 脚本）。

- [ ] **Step 1: 更新 `KONG_开发文档.md`**

- §3.7 冻结决定表追加 R-09（押注经济）并修订 R-07 语义（超限=判负→按最后一名失去押注、all-in 则出局）：
```markdown
| R-09 | 押注经济：起始 100 货币 + 1 灵魂币（health，默认 1）。每局开局确认后进入押注阶段：存活玩家押注（入场 20，+5 递增，上限 100 且 ≤ 当前货币）。结算：第一名(并列平分) 2× 返还 + 拿走垫底者全部押注；其他非最后玩家 1.5× 返还；最后一名押注全失；差额由系统补足。货币 < 入场时自动 all-in（押全部货币 + 灵魂币），all-in 且垫底则 health→0 出局。并列垫底同失。 | 货币驱动多局系列赛：低分获胜 + 押注风险，灵魂币只在 all-in 失败时扣。 |
```
- §3.6 Kongbaya 段落补注：首回合喊叫且非最低分者，按"最后一名"处理（失去押注，all-in 则出局）。
- §5.3 数据归属/规则段同步 health=1 灵魂币语义。

- [ ] **Step 2: 更新 `网络协议_V1.md`**

- 新增 `bet` RPC：`{ amount: Integer }`，`amount==0` 表示 all-in（仅货币<入场时合法）；`BET` 阶段；错误码 `INVALID_AMOUNT`。
- 快照：`phase` 增 `BET(9)`；`bet_ready`（已押注 seat 列表，BET 阶段存在）；`players[].currency`；`result.economy`（`{seat: 净变化}`）；`result.penalized` 语义=本局 all-in 垫底出局者。

- [ ] **Step 3: 更新 `功能实现文档.md` 与 `AI_AGENT_交接说明.md`**

- 功能实现文档：新增"货币经济（已完成）"说明（BET 阶段、`_settle_economy`、verify_economy 计数）；回归基准补 `verify_economy`。
- 交接说明：§2 基线补押注经济摘要；§8 验证命令补 verify_economy 一行；§5 状态机表加 BET(9)。

- [ ] **Step 4: 更新 `tools/run_all_tests.sh`**

`run_single "series"` 行之后加：

```bash
run_single "economy"   res://tests/verify_economy.tscn
```

- [ ] **Step 5: 全量回归**

Run: `tools/run_all_tests.sh --quick`
Expected: 全部 PASS（含 verify_economy；`verify_series`/`settlement`/`reconnect`/`kongbaya` 在 Task 2 已同步 BET 语义后保持全绿）。

- [ ] **Step 6: 提交**

```bash
git add docs/KONG_开发文档.md docs/网络协议_V1.md docs/功能实现文档.md docs/AI_AGENT_交接说明.md tools/run_all_tests.sh
git commit -m "doc: 押注经济规则/协议/功能文档同步 + 回归接入 verify_economy"
```

---

## Self-Review

**Spec 覆盖核对：**
- §4 资源（10→100 货币 + 灵魂币 health=1）→ Task 1 常量/默认值。
- §4 押注（默认2→入场20、可调、奖池零和→系统补足）→ Task 1/2（用户确认的新规则：胜者 2×+垫底者押注、安全 1.5×、系统补足）。
- §4 all-in（货币<入场自动，输→health→0）→ Task 1 `_settle_economy` + Task 2 `_server_bet`。
- §4 把末排名（胜场→货币）→ M1 已实现，货币流动后自然生效。
- 用户要求新 UI 优先 `.tscn` → Task 3 `bet_panel.tscn` 场景实体。

**占位符扫描：** 无 TBD/TODO；所有代码步骤给出具体 GDScript。

**类型一致性：** `bets[seat]={amount,all_in}`、`_settle_economy(winners, last_group)`、`Phase.BET`、`request_bet`/`_server_bet`/`_commit_bet`、`_start_bet_phase`、`BetPanel.setup(state, on_confirm)` 在各任务间一致；`setup` 追加 `economy` 参数带默认值，旧调用兼容。

**范围：** 仅 Milestone 2（押注经济）。商店盲拍（M3）/遗物（M4）为后续独立计划。注意 `_server_initial_ready` 语义变更（就绪→BET→TURN_DRAW）会波及 `verify_series` 的就绪断言，Task 2 需同步更新这些断言。