# KONG 功能实现文档（开发实现层 · 功能分配与待办）

> **本文档职责**：回答"开发者/Agent 应该实现什么、在哪里实现"。只做**功能分解与文件分配** + **待开发清单**。工程现状/架构/不变量见 `AI_AGENT_交接说明.md`，规则权威见 `KONG_开发文档.md`，本文件不重复定义规则。

## 一、功能分配表（16 Feature ↔ 文件）

| Feature | 文件 | 状态 |
|---|---|---|
| 01 Card | `scenes/ui/card.tscn` + `card_view.gd` | ✅ 完成（含图片素材） |
| 02 Deck | `game_state.gd`（`_create_deck`/`_draw_from_deck`） | ✅ 内嵌 |
| 03 Player Hand | `game_state.gd`（`players`/`cards`） | ✅ 内嵌 |
| 04 Game State | `game_state.gd` | ✅ 完成 |
| 05 Turn | `turn_system.gd` | ✅ 完成 |
| 06 Draw/Discard | `game_state.gd` | ✅ 内嵌 |
| 07 Effects | `effect_system.gd` | ✅ 完成 |
| 08 Peek | `peek_system.gd` | ✅ 完成 |
| 09 Swap | `swap_system.gd` | ✅ 完成 |
| 10 Slap | `slap_system.gd` + `duel_bar.gd` | ✅ 完成（含比拼 SLAP_DUEL、贴牌动画事件 slap_penalty/resolved/gift） |
| 11 Kongbaya | `kongbaya_system.gd` | ✅ 完成 |
| 12 Scoring | `score_system.gd` + `settlement_model.gd`（同步轮记分） | ✅ 完成 |
| 13 Hidden Info | `hidden_info.gd` | ✅ 完成 |
| 14 Lobby | `network.gd` + `lobby_view.gd` | ✅ 完成 |
| 15 Network | `network.gd` | ✅ 完成 |
| 16 UI/Presentation | `card_view.gd`/`card_factory.gd`/`main.gd` 等 | ✅ 已拆分 |

> 完整文件树与职责见 `AI_AGENT_交接说明.md` 第 4 节。

> **系列赛框架（已完成）**：把单局扩展为多局系列赛（房主开局设定 `match_limit` X，2-10 默认 5）。落点：`network.gd`（`_server_start_match(match_limit)` 存 `run.match_limit`、`_series_finished()` 把末判定、`GAME_OVER` 非把末 Timer 自动开下一局）、`series_ranking.gd`（`SeriesRanking.final_ranking()` 把末总排名：胜场降序→同胜场货币降序，淘汰不入榜）、`_guard_spectator()`（出局观战守卫接入全部动作 RPC，`eliminated==true` 仅公开信息不可操作）、`_is_alive()`（`health==0` 出局，扣血来源见名次奖励结算 R-12）。规则见 `KONG_开发文档.md` §3.7 R-08；协议见 `网络协议_V1.md`；测试 `verify_series.gd`。

> **名次奖励经济（已完成，R-12）**：取消开局押注阶段（`Phase.BET`（值 9）保留为弃用值，流程不再进入；开局记忆全员确认后直接进入正式回合）。落点：`KongRules` 常量（`START_CURRENCY=100`、`START_HEALTH=2`、`RANK_REWARD_FIRST=40`、`RANK_REWARD_MIDDLE=10`），生命默认 2（`new_default_run`）；`game_state.gd`（`_settle_rewards` 结算：按名次由系统发钱——第一 +40/中间 +10/垫底 0，并列组按区间奖励平分、全员同分各 +10；垫底（含并列）扣 1 生命，`health→0` 出局观战；手牌超限（R-07）与首回合 Kongbaya 失败者按垫底处理；货币不扣、下限 0，出局者保留货币；`last_result.rewards = {SeatId: {money, life_lost}}`/`penalized`）；`hidden_info.gd`（快照移除 `bet_ready`，`players[].currency`/`health` 公开）；每玩家区域手牌右侧金钱/生命 HUD：`scripts/ui/coin_icon.gd`（程序化铜钱）、`scripts/ui/life_icon.gd`（程序化心形）、`scripts/ui/stat_hud.gd`（`setup(state, seat)` 填充）、`game_view.gd`（定位挂载，名字标签不再显示货币）；`settlement_page.gd`（结算页展示"本局奖励"）。规则见 `KONG_开发文档.md` §3.7 R-12；协议见 `网络协议_V1.md`；测试 `verify_economy.gd`（29/29）。

> **商店固定价购买（已完成）**：系列赛非把末结算后进入 `Phase.SHOP`（值 10），展示至多 3 件随机遗物（v1 池 2 件，各固定价 50）。每位存活玩家**限购 1 件**：点击遗物且货币 ≥ 售价即扣款、遗物入库并记持有者 `run_state.relic_owners[relic_id]=seat`，同件**先到先得**（售出后他人不可再买）；也可「跳过购买」。全员完成（购买或跳过）→ 开下一局；货币不足只能跳过。落点：`scripts/core/relics.gd`（`Relics` 数据 + `pool()`/`def_by_id()`，`RELIC_PRICE=50`、`JOKER_TRANSFORM_ID`/`GUARD_SHIELD_ID` 常量）；`game_state.gd`（`shop`/`shop_result` 字段、`_start_shop`/`_pick_shop_offers`/`_deal_next_match`、`request_shop_buy`/`request_shop_skip` RPC、`_server_shop_buy`/`_server_shop_skip`、`_shop_submitted`/`_shop_all_done`、`RejectCode.INVALID_OFFER`）；`hidden_info.gd`（`_phase_name`、快照 `shop`（`offers:[{id,name,price,sold_by}]` + `done`）/`shop_result`/`run.relics`/`run.relic_owners`）；`scenes/ui/shop_panel.tscn` + `shop_panel.gd`（点击遗物购买 / 已售出·货币不足·已完成状态 / 「跳过购买」按钮）；`main.gd`（`PHASE_SHOP`、开/刷新/关面板、`_on_shop_buy`/`_on_shop_skip`、`_show_shop_result_toast` 一次性结果提示）；`settlement_page.gd`（非把末 footer"即将进入商店…"）。规则见 `KONG_开发文档.md` §3.7 R-10；协议见 `网络协议_V1.md`；测试 `verify_shop.gd`（23/23）。

> **遗物效果 v1（已完成，M4）**：`Joker 变换`（持有者在抽牌阶段抽到 Joker（抽牌堆或弃牌堆顶）可选变换为任意牌，`request_joker_transform` RPC；未变换时其手中 JOKER 分值 +2，`_relic_card_value` 统一结算/揭示）与 `防守护盾`（购买后下一局开局随机护盾持有者一格并消耗，`_apply_match_start_relics`；该格免疫其他玩家贴牌与 J/Q 交换，`RejectCode.PROTECTED`；快照槽位 `protected`；客户端 `JokerTransformPanel` + 护盾◈ 标记）。落点：`scripts/core/game_state.gd`（`_apply_match_start_relics`/`_is_protected`/`_is_relic_owner`/`_relic_card_value`/`_joker_bonus_map`/`_server_joker_transform`、`_server_q_decision` 护盾拒绝、`players[].protected_slot`）；`score_system.gd`（`calculate_ranking` 加 `joker_bonus` 参数）；`slap_system.gd`/`effect_system.gd`（`attempt`/`_blind_swap`/`_start_queen` 加 `PROTECTED` 拒绝）；`hidden_info.gd`（快照槽位 `protected` + 揭示卡 value 用 `_relic_card_value`）；`game_view.gd`（护盾受保护格紫色描边光晕 + ◈ 紫角标 + "变换 Joker" 按钮 + 每玩家区域左侧挂载 `RelicBar`）；`main.gd`（Joker 变换面板开/关/确认）；`scenes/ui/joker_transform_panel.tscn` + `joker_transform_panel.gd`；**遗物 UI**：`scripts/ui/relic_icon.gd`（程序化盾/星/空槽图标）、`scripts/ui/relic_bar.gd` + `scenes/ui/relic_bar.tscn`（物品栏：3 槽 + `n/3` + Hover Tooltip，纯展示读快照公开字段）、`shop_panel.gd`（商店遗物图标复用）。规则见 `KONG_开发文档.md` §3.7 R-11；协议见 `网络协议_V1.md`；测试 `verify_relics.gd`（19/19）。

## 二、待开发 / 待验证功能

### A. 待完善
| 功能 | 说明 |
|---|---|
| hint 名字加粗 | 当前 Label 不支持部分加粗；如需可用两个 Label 并排（普通文本 + 加粗名字） |
| Q 交换动画 | 玩家间交换(J)已做；Q 交换的对方信息在 client 端不可靠，暂未加轨迹动画，需 server 广播 q_context 或 client 记录目标 |
| 能力失败时大牌恢复 | 能力被 server 拒绝时，大牌保持隐藏（`_pending_hidden_for_ability`），需在失败时恢复显示以便重试 |

### B. 待验证（双开/手动）
| 功能 | 说明 |
|---|---|
| 7/8、9/10、J、Q 分支实测 | 双开验证看牌翻转、J/Q 交换动画 |
| 贴牌比拼（SLAP_DUEL）GUI 手动 | 逻辑已 headless 验证（verify_duel）；双开按 **O** 开调试贴牌 → 双贴进比拼 → 空格/STOP 停止 → 看胜者；关闭 O 恢复正常判定 |
| Kongbaya / 最终行动 / 三层同分 / 首回合惩罚 | 逻辑已 headless 验证（`verify_kongbaya` 15/15：最终轮结算、重复喊叫拦截、首回合标记）；双开端到端仍待验证 |
| Joker 红黑映射 | 确认 Joker1/Joker2 哪个是红（当前默认 red→Joker1） |

### C. 未来功能（依赖基础稳定后）
| 功能 | 归属文件 |
|---|---|
| King 能力（若加入） | `effect_system.gd` + `game_interaction.gd` |
| 更多遗物 / 全局变体 | 具体钩子落点：`game_state.gd`（`_apply_match_start_relics` / `_relic_card_value` / `_server_*` 效果 RPC）+ `scripts/core/relics.gd`（数据池）；v1 两件效果（Joker 变换 / 防守护盾）已完成 |

> **结算页（已完成）**：`settlement_model.gd`（同步轮记分：`layout_order`/`rounds`/`ranking_states`）+ `scenes/ui/settlement_page.tscn`（场景实体骨架）+ `settlement_page.gd`（逐张翻牌、实时排名重排、冠军特效）。**棋盘卡牌结算时初始背面，由结算页 `on_flip` 回调经 `main._on_settlement_flip` → `CardView.flip_reveal` 逐张联动翻面**（与行内记分同一时刻）；「再来一局」RPC 见 `网络协议_V1.md`；测试 `verify_settlement.gd`（38/38）。

> **断线重连（已完成）**：阶段一 seat 身份（`players` 以 seat_id 为 key，`peer_id` 仅当前连接）+ 离线标记 + 条件暂停 + 房主踢出/中止/解散；阶段二 `player_token` 认领重连 + 定向补回手牌面/待处理牌。详见 `AI_AGENT_交接说明.md` 不变量 #9 与 `网络协议_V1.md`。

> **网络条件注入测试层（已完成）**：`scripts/net/proxy_relay.gd`（UDP 中继：监听 7011 → 转发真实服务器 7007，在**数据报层**注入 丢包/延迟/抖动/黑障；由 host 测试进程挂载，客户端连中继即可，**生产代码零改动**）+ `tests/verify_proxy.gd/.tscn`（双实例场景：`baseline`/`latency`/`loss`/`jitter` + `reconnect` 拔网线→token 重连）+ `tools/run_all_tests.sh`（一键全量回归，`--quick` 只跑单进程核心）。语义边界：丢包/乱序/重复发生在数据报层会被 ENet 可靠通道吸收（重传/排序/去重），故验证的是"链路物理特性（延迟/丢包/抖动/中断）下的健壮性"；RPC 层重复请求幂等由 `verify_protocol` 拒绝校验覆盖。每个场景含"人话"说明与失败提示，详见 `AI_AGENT_交接说明.md` §8.1。

> 音效已实现：`audio_manager.gd`（autoload）+ `settings_menu.gd`（ESC 设置：音量/静音/主题/语言，持久化到 `user://settings.cfg`）。Kongbaya 铃铛（`Bell.mp3`，全员广播）+ 结算胜利声（`Winner01-04` 随机，延迟到结算页冠军时刻播放）。

## 三、开发约定

- **服务器权威不变**：所有规则状态改动在 `game_state.gd` 或由其广播；UI 只做投影。
- **回归基准**：`verify_protocol`(38/38) + `verify_swap`(10/10) + `verify_duel`(31/31) + `verify_reconnect`(41/41) + `verify_kongbaya`(15/15) + `verify_settlement`(38/38) + `verify_series`(41/41) + `verify_economy`(29/29) + `verify_shop`(30/30) + `verify_hint`(8/8) + `verify_net` 双实例 + `verify_proxy` 全场景（baseline/latency/loss/jitter/reconnect）双实例，每次改动保持全绿。**一键全量**：`tools/run_all_tests.sh`。`verify_relics` 随 M4 接入。
- **拆分类不做行为变更**：只搬代码，不改变判定/流程。
- **不启动 GUI**：用 headless unit test 验证（见交接说明第 8 节）。
