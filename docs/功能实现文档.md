# KONG 功能实现文档（开发实现层 · 功能分配与待办）

> **本文档职责**：回答"开发者/Agent 应该实现什么、在哪里实现"。只做**功能分解与文件分配** + **待开发清单**。工程现状/架构/不变量见 `AI_AGENT_交接说明.md`，规则权威见 `KONG_开发文档.md`，本文件不重复定义规则。

## 一、功能分配表（16 Feature ↔ 文件）

| Feature | 文件 | 状态 |
|---|---|---|
| 01 Card | `scenes/ui/card.tscn` + `card_view.gd` | ✅ 完成（含图片素材） |
| 02 Deck | `game_state.gd`（`_create_deck`/`_draw_from_deck`） | ✅ 内嵌 |
| 03 Player Hand | `game_state.gd`（`players`/`cards`） | ✅ 内嵌 |
| 04 Game State | `game_state.gd` | ✅ 完成 |
| 05 Turn | `turn_system.gd` | ✅ 完成 |
| 06 Draw/Discard | `game_state.gd` | ✅ 内嵌 |
| 07 Effects | `effect_system.gd` | ✅ 完成 |
| 08 Peek | `peek_system.gd` | ✅ 完成 |
| 09 Swap | `swap_system.gd` | ✅ 完成 |
| 10 Slap | `slap_system.gd` + `duel_bar.gd` | ✅ 完成（含比拼 SLAP_DUEL） |
| 11 Kongbaya | `kongbaya_system.gd` | ✅ 完成 |
| 12 Scoring | `score_system.gd` | ✅ 完成 |
| 13 Hidden Info | `hidden_info.gd` | ✅ 完成 |
| 14 Lobby | `network.gd` + `lobby_view.gd` | ✅ 完成 |
| 15 Network | `network.gd` | ✅ 完成 |
| 16 UI/Presentation | `card_view.gd`/`card_factory.gd`/`main.gd` 等 | ✅ 已拆分 |

> 完整文件树与职责见 `AI_AGENT_交接说明.md` 第 4 节。

## 二、待开发 / 待验证功能

### A. 待完善
| 功能 | 说明 |
|---|---|
| hint 名字加粗 | 当前 Label 不支持部分加粗；如需可用两个 Label 并排（普通文本 + 加粗名字） |
| Q 交换动画 | 玩家间交换(J)已做；Q 交换的对方信息在 client 端不可靠，暂未加轨迹动画，需 server 广播 q_context 或 client 记录目标 |
| 能力失败时大牌恢复 | 能力被 server 拒绝时，大牌保持隐藏（`_pending_hidden_for_ability`），需在失败时恢复显示以便重试 |

### B. 待验证（双开/手动）
| 功能 | 说明 |
|---|---|
| 7/8、9/10、J、Q 分支实测 | 双开验证看牌翻转、J/Q 交换动画 |
| 贴牌比拼（SLAP_DUEL）GUI 手动 | 逻辑已 headless 验证（verify_duel）；双开按 **O** 开调试贴牌 → 双贴进比拼 → 空格/STOP 停止 → 看胜者；关闭 O 恢复正常判定 |
| Kongbaya / 最终行动 / 三层同分 / 首回合惩罚 | 端到端验证 |
| Joker 红黑映射 | 确认 Joker1/Joker2 哪个是红（当前默认 red→Joker1） |

### C. 未来功能（依赖基础稳定后）
| 功能 | 归属文件 |
|---|---|
| King 能力（若加入） | `effect_system.gd` + `game_interaction.gd` |
| Roguelike 遗物 | `run_modifier.gd` + `effect_system.gd`（先填遗物设计卡） |
| 音效 | 新增 `audio.gd` |
| 结算动画 | `reveal_controller.gd` 扩展 |

## 三、开发约定

- **服务器权威不变**：所有规则状态改动在 `game_state.gd` 或由其广播；UI 只做投影。
- **回归基准**：`verify_protocol`(36/36) + `verify_swap`(10/10) + `verify_duel`(23/23) + `verify_net` 双实例，每次改动保持全绿。
- **拆分类不做行为变更**：只搬代码，不改变判定/流程。
- **不启动 GUI**：用 headless unit test 验证（见交接说明第 8 节）。
